---
description: >-
  生成或修改 K12 教学 HTML 时，自动遵循 teaching-page-workflow 全流程
  + teaching-page-v2 执行层（无需 @teaching-page）。
globs: pages/**/*.html
alwaysApply: true
---

# Teaching Page Harness（Workflow + v2）

## 何时启用

教学页面、静态课件、互动课件、课程游戏、课堂演示、多页课件；在 `pages/` 下新建/修改教学 HTML。

## 何时不要启用

- 架构讨论、研究资料、汇报文档
- 与教学 HTML 无关的前端任务
- 只改 `courseware-shell.js` 引擎（读 `teaching-page-v2/reference.md`）

## 执行

```
Read teaching-page-workflow/SKILL.md
  → Phase 0 route.md
  → phases/01–06
  → Phase 5 调用 teaching-page-v2/execute.md
  → Write pages/<slug>/ + .cursor/workflow/<slug>/
```

**全流程在本地**；服务端仅提供 Agent 运行时。

## 契约

- 过程产物：`.cursor/workflow/<slug>/*.yaml`
- 最终交付：**`<slug>.zip`** + `pages/<slug>/`
- 多页：`package-scorm.sh`（四文件）
- 游戏：`package-scorm-game.sh`（三文件）
- 执行层绑定：`teaching-page-workflow/execution-binding.md`（当前 v2）
- 骨架：`teaching-page-v2/templates/`（随执行层版本，不跨版本共享）
- SCORM：见 `teaching-page-v2/scorm.md`

## 修改已有课件

Read 目标 HTML → 增量 Phase 5 → Phase 6 重打包。

## 归档

`teaching-page-v1/` — 旧 harness，仅回归对照。
