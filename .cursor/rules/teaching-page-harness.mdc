---
description: >-
  生成或修改 K12 互动课件 HTML 时，自动遵循 teaching-page-v2 Harness
  （无需 @teaching-page）。单页/多页形态由服务端上游保障，Harness 不判定。
globs: pages/**/*.html
alwaysApply: true
---

# Teaching Page Harness v2

## 何时启用

教学页面、互动课件、课堂演示、答题页、多页课件；在 `pages/` 下新建/修改教学 HTML；演示 Harness。

## 何时不要启用

- 架构讨论、研究资料、汇报文档
- 与教学 HTML 无关的前端任务
- 只改 `courseware-shell.js` 引擎（读 `teaching-page-v2/reference.md`）

## 执行

```
Read teaching-page-v2/SKILL.md → execute.md → Write pages/<slug>/
```

**禁止**在 Harness 内判定 single/multi 或维护决策文件（服务端职责）。

## 契约

- 产出：**`<slug>.zip`**（最终 LMS 交付）+ 开发目录四源文件
- 打包：`bash teaching-page-v2/assets/package-scorm.sh pages/<slug>`
- 骨架：`teaching-page-shared/templates/courseware-starter.html`
- SCORM：见 `teaching-page-v2/scorm.md`；测验页必调 `__cwScormReport`
- `page-shared` / `page-data` / `.page-container` / spec 注释
- 画布 `#eef1f5`，壳无外框阴影
- 多端：PC + iPad（`responsive.md`）

## 修改已有课件

Read 目标 HTML → 只改用户要求部分 → 保留 spec、壳路径、配色。

## 归档

`teaching-page-v1/` — 旧 harness，仅回归对照。
