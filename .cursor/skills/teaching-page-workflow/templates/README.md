# Workflow 过程产物示例

生成时写入 `.cursor/workflow/<slug>/`（本地过程文件，可选提交 git）。

| 文件 | 说明 |
|------|------|
| `route.yaml.example` | Phase 0 路由 |
| `intake.yaml.example` | Phase 1 接单 |
| `outline.yaml.example` | Phase 2 大纲（interactive） |

已发布 HTML 示例：`pages/acid-base-fx/`（互动课件四页 + SCORM ZIP）。

各 Phase 字段说明见 [phases/](../phases/) 与 [execution-binding.md](../execution-binding.md)。
