# v2 模板（随本版本能力绑定）

**禁止**跨版本引用。Workflow Phase 5 调用 v2 时，只读本目录。

| 文件 | 用途 |
|------|------|
| `courseware-starter.html` | 多页 Host + page-shared（static / interactive） |
| `game-starter.html` | 单页课程游戏 + 状态机骨架 |
| `imsmanifest.template.xml` | 多页 SCORM 四件套清单 |
| `imsmanifest-game.template.xml` | 游戏 SCORM 三件套清单 |
| `single-page-starter.html` | 单页非游戏（归档参考，Workflow 默认不用） |

v1 模板见 `teaching-page-v1/templates/`（无 SCORM / 无游戏）。
